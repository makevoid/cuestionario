!!!
%html
  %head
    %meta{ charset: :utf8 }
    %title Cuestionario
    %link{ href: "css/main.css", rel: "stylesheet" }
    %script{ src: "/js/underscore.js", type: "text/javascript" }
    %script{ src: "/js/jquery.js", type: "text/javascript" }
    :javascript
      project_names = #{JSON.generate(selects[:project_names]).inspect}
      partners = #{JSON.generate(selects[:partners]).inspect}
      activities = #{JSON.generate(selects[:activities]).inspect}
      objectives = #{JSON.generate(selects[:objectives]).inspect}
    %script{ src: "/js/main.js", type: "text/javascript" }
  %body
    = yield